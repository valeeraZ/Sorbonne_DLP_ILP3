function f(nb, msg)(
    let i = 0 in
        while i < nb do(
            print(msg);
            i = i + 1;
            yield();
        )
);

let c1 = costart f(5,"A") in

let i = 0 in
    while i < 10 do(
        print(i);

        resume(c1);


        i = i + 1
    )